# Fuzon

Fuzon is used by the client as a terminology matching service.
It can also be embedded in the client as an optional dependency, but running it server-side is faster.

Fuzon is completely optional and is only used by the client for metadata code suggestions.
